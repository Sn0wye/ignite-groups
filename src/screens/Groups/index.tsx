import { Header } from '@components/Header';
import { Text } from 'react-native';

import { Container } from './styles';

export const Groups = () => {
  return (
    <Container>
      <Header />
      <Text>Group</Text>
    </Container>
  );
};
